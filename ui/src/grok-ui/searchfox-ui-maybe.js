/**
 * TODO: A mechanism that checks whether this is an iframe load of a searchfox
 * source file page that does not actually need to load any JS logic because
 * the parent frame already has the logic.
 */
